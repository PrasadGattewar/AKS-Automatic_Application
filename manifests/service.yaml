apiVersion: v1
kind: Service
metadata:
  name: aksautoapp
  namespace: devhub
  labels:
    app.kubernetes.io/name: aksautoapp
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: aksautoapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80